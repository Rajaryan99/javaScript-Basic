let drink = "coffee"

let cups = Math.random() > 0.5 ? 10 : 5;

let name = "raj Aryan" //“Type inference in TypeScript is the ability of the compiler to automatically determine the type of a variable based on its value, reducing the need for explicit type annotations.”

let whichCoffee: string = "black coffee || Expresso without milk "

